# Design

```
screenshots/  captures of the built prototype, dated
concepts/     explorations that are not the current direction
references/   inspiration and competitor work
```

The prototype itself is the living design artefact — see `website/`. Use this folder
for things that need to be looked at rather than run.

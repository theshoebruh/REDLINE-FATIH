# Colours

`palette.json` is the source of truth for intent. The live values the site actually
renders live in `website/src/styles/tokens.css` — change them there and the whole
site re-skins, since nothing hardcodes a hex outside that file.

Brand identity is not final. Expect the red to move.

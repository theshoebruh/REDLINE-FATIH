UI icons. Currently the site uses type characters (arrows, plus/minus, ✕) rather
than an icon set, so this is empty on purpose. If an icon set is added, keep it to a
single stroke weight and drop SVGs here.

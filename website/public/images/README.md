```
brand/      logo variants + the homepage hero
products/   <slug>/ per product, web-sized
```

Source material and print-resolution files live in `../../products/` and
`../../brand/`, not here. This folder is only what the browser loads.
